var GameBoyController = (function(window, document) {
  window.onkeydown = function(e) {
    var keycode = e.keycode;
    var direction = null;

    switch(keycode) {
      case 37:
        break;
      case 38:
        break;
      case 39:
        break;
      case 40:
        break;
    }
  }
}());
GameBoyController.keyControl(window, document);
